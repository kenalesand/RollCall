Console.WriteLine("Hello, World!");

// Periodically (event?) poll the wormhole exit directory for roll files

// Extract file info from each roll file.
// HashSet<string, OrderedList<long, List<FileRecord>>> AllPendingFiles
// Should create a Task for each sequence scope

